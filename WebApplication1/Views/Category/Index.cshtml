@using WebApplication1.Entities;

@model List<Category>


<div class="row">
    <div class="col-md-12">
        <a asp-action="Create" asp-asp-controller="Category" class="btn btn-success">
            Create
        </a>
    </div>
    <div class="col-md-12">
        <table class="table">
            <thead>
                <tr>
                    <td>Manage</td>
                    @* <td>Id</td> *@
                    <td>Category Name</td>
                    <td>Contains</td>
                    <td>Info</td>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            <a asp-action="Edit" asp-controller="Category" class="btn btn-warning" asp-route-id="@item.Id">
                                Edit
                            </a>
                            <a asp-action="Delete" asp-controller="Category" class="btn btn-danger" asp-route-id="@item.Id">
                                Delete
                            </a>
                        </td>
                        @* <td>@item.Id</td> *@
                        <td>@item.CategoryName</td>
                        <td>@item.Contacts.Count</td>
                        <td>
                            <a asp-action="Details" asp-controller="Category" class="btn btn-primary" asp-route-id="@item.Id">
                                View
                            </a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
